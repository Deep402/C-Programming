// a. Anything that can be done with ileration, can be done with recursion and 
// vice-versa.
// b. Recursion can sometimes gives the msot simple solutions.
// c. Base Case is the condition with stops recusrions and thus its very imp. to be defined.
// d. Iteration has infine loop & Recusrion has stack overflow(the code keeps running with a infinte loop and stops after memory is full).